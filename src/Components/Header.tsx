import { AppBar, Toolbar, Typography } from "@mui/material";

export default function Header(): JSX.Element {
  return (
    <AppBar>
      <Toolbar>
        <Typography>Lk - Loja Nerd </Typography>
      </Toolbar>
    </AppBar>
  );
}
